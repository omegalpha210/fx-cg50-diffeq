# Event / Diagnostics captures

Reproduce these two lossless 396×224 PNGs with `python3 tools/capture_events.py`
after the host build. Production C UI/renderer, host framebuffer; not calculator
photos or a CPU emulator. No manual imagery or generated mockups are included.

- event-settings.png: y'=y, y(0)=1, RK45, Event y-10 / RISING / STOP.
- solver-diagnostics.png: the same Graph run, INFO scrolled to numerical work.

HARDWARE TEST REQUIRED for LCD/key/target-math validation.
